# Local Netlify folder
.netlify

# Local data & model files (downloaded from Hugging Face at runtime)
# NOTE: Files are stored in HF Space via Git LFS but ignored locally to prevent accidental commits
bible_bilingual_metadata.pkl
bible_bilingual_vector_cuv.npy
bible_bilingual_vector_esv.npy

# SQLite database (per-environment)
bible_sphere.db

# Build artifacts
emotion-sphere-ui/dist.zip

# Python cache
__pycache__/
*.py[cod]
*.pyo
.venv/

# OS files
.DS_Store
__pycache__/

# Environment variables (secrets)
.env
.env.local

# Runtime-generated local state
visit_stats.json
artifact_manifest.json
evaluation/reports/
*.npy
bible_bilingual_vector_cuv.npy
bible_bilingual_vector_esv.npy
bible_bilingual_vector_cuv.npy
bible_bilingual_vector_esv.npy

# Next.js bible-map app
/.next/
/node_modules
/.env.local
_ebooks_backup/
_hymns_backup/

# Frontend moved to independent repo bible3dsphereWeb
emotion-sphere-ui/

# ============================================================
# Repo hygiene — untracked cruft (kept on disk, out of git)
# ============================================================
# JetBrains IDE
.idea/

# Old Next.js frontend (superseded by independent repo bible3dsphereWeb)
archive/

# One-off / experimental root scripts (not imported by backend, not in Docker image)
biblical_film_studio.py
video_studio_server.py
generate_david_goliath.py
fix_sphere.py
commit_b10_b12.sh

# Stray permission-probe / write-test artifacts
_perm_probe.txt
backend/_perm_probe.txt
.write_test_xyz

# Duplicate pytest config (canonical one is backend/pytest.ini)
backend/tests/pytest.ini
