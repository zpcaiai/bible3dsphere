# ============================================================
# DEPRECATED / ORPHAN — do not use.
#
# This root requirements.txt is not referenced by any deploy target
# (the Dockerfile and render.yaml both install backend/requirements.txt).
# It previously carried stale, conflicting, and implausible pins
# (e.g. pandas==3.0.2, a different edge-tts version than the backend).
#
# The single source of truth for backend dependencies is:
#     backend/requirements.txt
#
# This file is intentionally emptied of real requirements. It should be
# removed from the repo entirely (`git rm requirements.txt`) once the
# stale .git/index.lock is cleared — see the deployment notes.
# ============================================================
